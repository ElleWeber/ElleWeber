# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a GitHub profile repository (`ElleWeber/ElleWeber`). The `README.md` in this repo is special — GitHub automatically displays it on the profile page at github.com/ElleWeber.

## Structure

The repository contains only one meaningful file:

- `README.md` — the public GitHub profile page content

## Conventions

- The README uses GitHub-flavored Markdown and emoji shortcodes (e.g. `👋`, `🌱`).
- The hidden HTML comment block at the bottom (`<!--` ... `-->`) is part of GitHub's default template and can be kept or removed.
- Contact email: elodieweber@elodieweber.consulting
- Pronouns: She/Her
